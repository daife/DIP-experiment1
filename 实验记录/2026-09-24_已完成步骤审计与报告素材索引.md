# 已完成步骤审计与报告素材索引

- 审计日期：2026-09-24
- 依据：原始 PDF 第 2～4 页的步骤一至五、步骤六预标注相关文字，配套详细说明第八至十三节；`TODO.md` 只用作进度索引。
- 范围：`TODO.md` 已勾选的步骤一至五及步骤六的 64 张预标注基准。后续关键点训练、评价、Demo 尚未验收。
- 方法：检查源码与存档文件，运行环境验证、28 项单元测试及步骤三数据验证；核对结果 JSON、模型参数、人工复核表和图像路径；直接查看步骤二通道图、步骤三编号总览、步骤四困难负样本总览、步骤五检测预览。未逐张重新复核 4,800 张基本裁剪。

## 逐项结论

| 步骤 | 规格验收与证据 | 结论及报告表述边界 |
|---|---|---|
| 一：环境 | `uv 0.11.9`；`.venv` 为 CPython 3.12.13。`scripts/verify_environment.py` 对 NumPy、OpenCV、scikit-learn、Matplotlib、Pillow、tqdm、anime-face-detector/PyTorch 均返回 `ok`。`scripts/setup_environment.ps1` 和 `requirements.txt` 保留安装流程。 | 符合。环境版本可能变化，报告引用本次记录的版本和日期。 |
| 二：11 通道 | `src/channels11.py` 以整数中间量产生 11 张同尺寸 `uint8` 图；`tests/test_channels11.py` 独立标量公式、边界、小图和非法输入均通过。`results/step2_channels/` 有 6 张 2080×1430 的“原图 + 11 通道”图及 `samples.json`。阶段记录说明通道响应、干扰和 Cascade 作用。 | 符合。漫画复杂背景图在本地存在但因 Manga109 授权被忽略，交报告前须核对来源与署名。 |
| 三：检测训练集 | `scripts/verify_detection_dataset.py` 返回 8,400 张裁剪、4,800 张基本图、109 个来源组；作品级固定划分和仅 train 翻转有代码与清单支持。`review_summary.json` 为基本图 keep 4,658、reject 142，可用图含增强为 8,156；人工意见保留在 `datasets/annotations/corrected/`。50 张编号图在本地。 | 数据构建与既有复核流程符合。此次只抽看一张正样本编号表，不能把此次审计写成全量复核；不同画风、遮挡等覆盖可举例展示，尚无按类别统计。 |
| 四：Depth-2 Cascade | 模型存 3 个 Stage、每级 18 棵树及阈值；代码实现像素差、三判断节点、四叶分数、随机候选贪心搜索和 AdaBoost 更新。初版与回训结果、test/validation/train 各 Stage 通过数均有 JSON。240 个挖掘候选中只将已复核的 24 个回训；困难样本总览和至少 6 个误检原因留存。 | 算法、训练、挖掘流程符合。test **预裁剪**背景最终通过数 121→79，同时人脸 327→304；24 个加入的困难样本回训后仍全部通过。不能据此宣称整页检测准确率提高。开发中查看过 test 结果，最终泛化评价需在固定模型后重新设计。 |
| 五：多尺度与 NMS | `src/multiscale.py`、单元测试和 `results/step5_comparison.json` 覆盖 1.1/1.2/1.3、step 1/2、逐层尺寸/窗口/候选/耗时、坐标映射和 NMS；两张不同 validation 作品的 320×320 裁剪共 12 次运行。`results/step5_full_page.json` 含原生 1654×1170 页面 step 1/2 的逐层与原页框。12 张预览图在本地。 | 功能与比较符合。当前模型在页面预览有大量局部结构误报；对照中的最佳 IoU 只对锚定单脸，不能作为完整页面 Precision/Recall/F1。 |
| 六：64 张预标注基准 | `实验记录/2026-09-23_关键点预标注短程基准.md` 记载固定抽样、命令、64/64 成功、每图 28 点、吞吐量和 4 个越界点；自动 JSONL 与元数据留在本地。 | 只确认预标注吞吐量基准。`visibility` 尚未人工确认，28 点真值数据与回归器仍未完成；`TODO.md` 只勾选该基准是恰当的。 |

## 可用于后续报告的图与数据

| 用途 | 素材与复现 | 使用限制 |
|---|---|---|
| 11 通道结构与干扰 | `results/step2_channels/*.png`；`scripts/visualize_channels.py`；`samples.json` 保存原图哈希与裁剪参数。 | 五张可追踪的非 Manga109 图已纳入版本管理；漫画复杂背景图及其源图仅本地保存，展示前检查授权与来源。 |
| 正负样本构建与筛选 | 本地 `datasets/derived/step3_detection_v1/review_sheets/` 的 50 张编号图（另有 ZIP 归档）；`review_summary.json`、`usable_samples.jsonl`、`datasets/annotations/corrected/step3_crop_review.csv`。 | Manga109 图像不随仓库分发；编号表是人工复核过程证据，报告选图须标明样本 ID 和正负类别。 |
| 训练前后 Stage 保留量 | `results/report_evidence/step4_stage_retention.png`；从 `results/step4_initial_stage_stats.json` 和 `results/step4_stage_stats.json` 经 `scripts/plot_report_evidence.py` 生成。 | 图中为 test **裁剪分类**，同时呈现人脸保留损失；不能标作整页检测指标。 |
| 六个以上困难负样本 | 本地 `datasets/derived/step4_hard_negatives_v2/contact_sheet.png`；相应 ID/误检原因见步骤四记录及人工复核 CSV。 | 当前图可打开，来源为 Manga109；报告引用前检查授权与署名。 |
| 尺度与步长效率 | `results/report_evidence/step5_scale_stride.png`；从 `results/step5_comparison.json` 经同一绘图脚本生成。 | 两个固定裁剪、单次本机计时；图只说明工作量与耗时。 |
| 多尺度结果示例和失效案例 | 本地 `datasets/derived/step5_comparison/*scale1.2_step1.png` 等 12 图；原始数值见 `results/step5_comparison.json`，原生整页见 `results/step5_full_page.json`。 | 预览中绿框为锚定标注、红框为前 20 个 NMS 结果；误报明显，报告应如实呈现。 |

## 本次运行与后续缺口

- `.venv\Scripts\python.exe -m unittest discover -s tests -v`：28/28 通过。早期阶段记录中的“6 项”或“16 项”是当时的历史运行数，并非当前全套测试数。
- `.venv\Scripts\python.exe scripts/verify_environment.py`：`status=ok`。
- `.venv\Scripts\python.exe scripts/verify_detection_dataset.py`：8,400 张、来源组 109，验证通过。
- 已保存两张可再生成的原创统计图，并目视检查图中文字与数值可读。图片绘图只读取既存 JSON，没有重新训练、改写模型或重新计时。
- 步骤七仍需固定模型后的整页 IoU 检测指标、误报/漏检例图和效率结果；步骤六需人工校正关键点。最终报告选用 Manga109 图像时应遵守数据集展示要求，并核对本地忽略文件仍可用。
