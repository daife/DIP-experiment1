# 阶段记录：320 张关键点人工粗复核接收与文件整理

- 日期：2026-09-25
- 对应规格：原始 PDF 第 3 页步骤六要求固定 28 点顺序、遮挡点 `visibility=0`，配套详细说明第 13.2 节要求测试集人工检查。此次只接收**V/H/U 分类及模型坐标的人工粗复核**，没有人工精调坐标。
- 用户确认：在项目会话中明确答复“是，全部逐张核对并接受当前结果”，随后补充“这次只是简单的人工复核（没有精调坐标）”。这两条说明是本阶段人工范围的依据，不能扩写成精确坐标真值。

## 输入、整理与来源

- 固定 320 张预标注：`datasets/annotations/review_sets/landmark320/annotations.jsonl`，SHA-256 `db187f9ae74825615c1d137b18c28eafdad022230f2890c290c12d2ce7af0baa`，固定 split 240/32/48；源图清单和单图校验值见同目录 `review_set.json`。模型权重与随机种子见两份运行元数据。
- 用户放入 `corrected/` 的 `landmark28_review320.jsonl` 原文件 SHA-256 `56ce64034a043b318adb8b4c6be53eb50c798de1beeef2b8c975c6646147dc0b`。该文件逐条自述 `reviewer_claim: not_human_reviewed`、方法为 Codex 辅助视觉复核；因此先原样保存在 `review_sets/landmark320/assisted_review_v2.jsonl`，不能把原文件的元数据直接当成人工校正证明。
- 同时移入该目录的 `assisted_review_v2_summary.json` 为对应辅助复核汇总，其 `output_sha256` 与原文件匹配。汇总中的绝对路径是制作机器的历史路径，当前仓库以相对路径为准。
- `corrected/landmark28_schema.json` 与根级 `datasets/annotations/landmark28_schema.json` 字节相同，已删去副本；唯一规范仍在根级注释目录。
- 依据用户确认创建 `corrected/landmark28_review320_acceptance.json`，通过 `scripts/promote_landmark_review.py` 生成正式 `corrected/landmark28_review320.jsonl`。新文件将原辅助复核元数据保存在 `assisted_review` 字段，另以 `review.status=human_coarse_review_accepted` 标明人工接受范围；没有改动任何图片 ID、点坐标、置信度或 V/H/U 分类。正式 JSONL SHA-256 `f2304ab323cb547dcd36383ff2afd95bcdafc8ab139e06ff20265dd3b65019db`。

## 验证

```powershell
.venv\Scripts\python.exe scripts/promote_landmark_review.py
.venv\Scripts\python.exe scripts/verify_landmark_review.py
```

- `verify_landmark_review.py`：320 个唯一图片 ID 与固定批次完全一致；train 240、validation 32、test 48；每图 28 点；可见 V 8,669、可推定遮挡 H 74、不可定位 U 217；`visibility` 与 `coordinate_quality` 组合合法；模型分数未改；点坐标与原预标注比较 **0 处改变**。结果和原始数值保存在 `results/landmark_review320_coarse_stats.json`，可由上述命令重跑。
- 六张全 U：test 三张 `anime256:195162-10031.jpg`、`anime256:185146-9211.jpg`、`anime256:14531-5057.jpg`；train 三张 `anime256:61829-25391.jpg`、`anime256:27393-17071.jpg`、`anime256:44846-23554.jpg`。这些样本目前不能进入形状拟合或 NME；“全 U”表示本轮 28 点坐标全部不可靠，不等于断言画面无人脸。
- 直接打开并检查 `195162-10031.jpg`、`195162-10056.jpg`、`270509-16840.jpg` 及其余五张全 U 原图。可观察到全 U 图片仍有可辨的面部结构，说明这批标注宜作为粗筛与后续精调入口，不能将其全 U 解释为“28 个真实点全部被遮挡”；没有自行改写用户接受的点位判断。
- 输入的 `review_summary.json` 写着 `human_review_claimed=false`，这是**辅助复核阶段**的历史声明；人工接受是随后发生的独立事件。两层证据均保留，不互相覆盖。

## 限制与下一步

- 没有人工精调任何坐标。特别是 `H` 点的“可靠推定”沿用 HRNetV2 预测位置，虽经粗复核接受，不能当成可直接核验的遮挡点真实坐标。
- 当前 test 48 张均在用户声明的逐张粗复核范围内，但若要报告以精确人工真值为参照的 NME，应先逐点精调或至少针对 test 样本建立更严格的坐标验收；六张全 U 需复核是否排除并保留原因。`TODO.md` 中“形成可用的 28 点数据”仍不勾选。
- 后续训练可先以这批数据探索基线，但应分开报告“以粗复核预标注为参照的误差”和“以精调真值为参照的误差”，不能混称。
