# 阶段记录：Fern 风格关键点进阶对照

- 对应规格：原始 PDF 第 3 页步骤六的进阶方案；详细说明第 13.4 节局部像素差、二值编码或 Fern 叶节点、线性回归、多级形状更新。
- 运行命令：`.venv\Scripts\python.exe scripts/train_landmark_fern.py`。输入 `datasets/annotations/corrected/landmark28_review320.jsonl`，SHA-256 `f2304ab323cb547dcd36383ff2afd95bcdafc8ab139e06ff20265dd3b65019db`；图像位于 `datasets/annotations/review_sets/landmark320/images/`；均值形状 `results/landmark_mean_shape_coarse.json`，SHA-256 `5e3a2a97795d2d04a480e5f9cb86a02baa5a624ca0963fbdc8ab2a99b8dcbce7`。
- 固定 split：train 240、validation 32、test 48；来源组不交叉。原始划分种子 `experiment1-anime-face-v1`，补样种子 `20260925`，本次特征种子 `20260925`。仅以 train 可见点 V 作目标，6,550 点、237 张有有效目标；U/H 不作训练目标。坐标为人工粗核验接受、未精调的 HRNetV2 预标注。
- 配置：框归一化 256×256；每点 8 对局部像素差，偏移半径为框宽高的 0.075；每 4 个差值的正号组成一个 4-bit 编码，每点两个 Fern，展开为 32 维局部 one-hot 叶特征；逐点带掩码的 Ridge 更新 2 维残差，alpha=10；共 4 轮。实现为 `src/landmark_fern.py`。
- 模型 `models/landmark_fern4_visible_only.npz`，SHA-256 `ab68106808e152740ccda8b1db4354e05db9a55e252f7ef57730ac657292b470`；加载模型后的逐图原始数值 `results/landmark_fern_coarse_metrics.json`，含每图可见点数、NME、双眼中心或框对角线归一化方式及 H 点辅助误差。

| 方法 | Validation 可见点 NME | Test 可见点 NME |
|---|---:|---:|
| 四轮基础 Ridge，仅 V | 0.160016 | 0.155277 |
| 四轮 Fern 风格，仅 V | 0.191051 | 0.191047 |

Fern 配置在 validation 上未改善基础模型，因此后续统一接口采用基础 Ridge 仅 V 模型。该比较只覆盖固定的已裁剪 anime256 图和整图框；不是整页检测指标。test 数值已经查看，不能称为盲测。Fern 的符号编码损失了像素差幅度信息，这可能解释性能下降，但尚未做消融验证。无报告图像新增；表格原始数值由上述 JSON 和 `results/landmark_regression_coarse_metrics.json` 提供。下一步继续步骤七检测评价和步骤八集成。
