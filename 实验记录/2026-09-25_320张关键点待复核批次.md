# 阶段记录：320 张关键点待复核批次

- 日期：2026-09-25
- 对应规格：原始 PDF 第 3 页步骤六“28 点关键点回归”，配套详细说明第 13.2 节。PDF 未规定最低张数；320 张是本实验的基础规模选择，不是原文门槛。
- 当前状态：**预标注与本地复核材料已就绪，人工校正尚未开始**。不得将这批结果称为关键点真值或据此报告 NME。

## 输入、划分与模型

- 原始图片池：`datasets/raw/anime256/`，原始压缩包 `datasets/downloads/anime256.zip`，两者仍在本地忽略目录；来源项目为 <https://github.com/learner-lu/anime-face-dataset>。本批 320 张复核图片已复制到 `datasets/annotations/review_sets/landmark320/images/` 并纳入仓库，合计 8,796,304 字节；清单记录逐图 SHA-256。原始压缩包 SHA-256 `5e266c8954dc75f0d0bbf01e537e0097faba34c3cd7d7efbc7195bfabec07297`。
- 固定 split 来自 `datasets/manifests/images.csv` 的来源组划分（总划分种子 `experiment1-anime-face-v1`）。本批保留先前固定抽取的 64 张，另以种子 `20260925` 从未用来源组中补 256 张。各 split 目标：train 240、validation 32、test 48；实际一致。320 张涉及 319 个来源组；同组没有跨 split，补充样本每组最多一张。
- 模型：`anime-face-detector 0.1.0` 的 HRNetV2，权重 SHA-256 `e71271376406a743c01528a0460637fcc06e72aeeea583f85007cc72dc8b7a4a`；输入为完整 256×256 裁剪，跳过 YOLO，开启水平翻转测试；CPU。28 点顺序由 `datasets/annotations/landmark28_schema.json` 固定。

## 实际命令与输出

```powershell
.venv\Scripts\python.exe scripts/prepare_landmark_review_set.py select
.venv\Scripts\python.exe scripts/preannotate_landmarks.py --manifest datasets/annotations/review_sets/landmark320/supplement_selection.csv --limit 256 --seed 20260925 --run-name hrnetv2_review_256_v2
.venv\Scripts\python.exe scripts/prepare_landmark_review_set.py assemble
```

- `datasets/annotations/review_sets/landmark320/supplement_selection.csv`：固定补充的 256 张图片 ID、来源组与 split。
- `datasets/annotations/auto/hrnetv2_review_256_v2/`：本地忽略的新增自动标注和完整运行元数据。256/256 成功，无失败；批次耗时 31.97 秒，稳态均值 0.12488 秒/图；新增 7,168 个点中 2 个越界。模型与运行环境细节见 `run_metadata.json`。
- `datasets/annotations/review_sets/landmark320/annotations.jsonl`：合并的 320 张待人工复核自动标注，SHA-256 `b7046385b9d0bcd10213fd89c5715dbccb985d1876d40f8991fea5d1af6c968b`。
- `datasets/annotations/review_sets/landmark320/review_set.json`：每张复核图的仓库内相对路径、split、来源组、图片 SHA-256 与字节数，以及原始运行和选样文件的 SHA-256。320 张复核图合计 8,796,304 字节。
- 同目录 `pilot_run_metadata.json` 和 `supplement_run_metadata.json` 保存两次自动预标注运行参数、模型校验值、环境与耗时，采用固定 LF 换行以便随代码提交和校验。
- 人工复核工具默认读取该合并 JSONL，输出路径为 `datasets/annotations/corrected/landmark28_review320.jsonl`；当前尚无此文件。

首次预标注运行使用相对 `--manifest` 时，256 张推理结果已写出，但写元数据阶段因 `relative_to` 对相对路径报错。修正为解析绝对路径后以 `hrnetv2_review_256_v2` 完整重跑；不采用首次缺元数据的输出。

## 验证与限制

- 合并批次 320 个唯一图片 ID，train/validation/test 分别 240/32/48；8,960/8,960 个点 `visibility=null`，明确处于未复核状态。
- 自动预测共 6 个点越界：先前 64 张有 4 个，新增 256 张有 2 个。人工应按图像可见性决定是否修正，不直接裁剪或把模型置信度视作真值。
- 图片按 `review_set.json` 记录 SHA-256；编号示意图见 `results/landmark28_index.png`。本阶段没有生成检测或关键点性能图。后续人工复核的修改范围、遮挡点与排除样本要从实际校正 JSONL 统计，不能把尚未检查的图像写作人工通过。
- 按用户最新要求将 320 张复核图片纳入 Git；原始完整数据集和原始压缩包保持忽略状态。本次仅更新工作树，不推送远端。

## 下一步

人工按 `datasets/annotations/LANDMARK_REVIEW_GUIDE.md` 在本机逐图检查、保存。收到校正结果后，先验证 320 张记录及 test 全量复核，再计算平均形状、训练 3～5 轮 Ridge 形状回归并做 validation/test NME。
