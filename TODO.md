# 实验一 TODO

本清单按实验说明的八个步骤排列，只保留明确要求及完成要求所需的最低验收项。

## 步骤一：环境搭建

- [x] 安装 `uv`。
- [x] 使用 CPython 3.12 创建项目虚拟环境 `.venv`。
- [x] 安装 `requirements.txt` 中的依赖。
- [x] 运行环境验证，确认 NumPy、OpenCV、scikit-learn、Matplotlib 和关键点依赖可用。

运行命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/setup_environment.ps1
```

## 步骤二：复现并观察 11 通道特征

- [x] 参考 `cpu-face-hpc` 实现 `compute_11_channels(gray)`。
- [x] 输入为 `uint8` 灰度图，输出 11 个同尺寸 `uint8` 通道：原图、2×2 局部均值、跨 2 像素平滑、4 个短距离方向梯度、4 个长距离方向梯度。
- [x] 中间计算使用 `int16` 或 `int32`，不使用现成卷积库直接生成全部结果。
- [x] 最低验收：检查通道数量、形状、类型，并用小矩阵抽查通道公式。
- [x] 选择至少 6 张图像：2 张真实正面人脸、2 张近正面动漫人脸、1 张头发遮挡、1 张漫画复杂背景。
- [x] 绘制每张图的“原图 + 11 个通道”。
- [x] 说明各通道突出哪些结构、容易受到哪些干扰，以及 Cascade 为什么能选出有效弱分类器。

## 步骤三：构建动漫人脸检测训练集

- [x] 已整理 Manga109、LAP 和 anime256，并生成统一图片清单与标准化人脸框。
- [x] 已按来源组固定 Train 75% / Validation 10% / Test 15%，避免同源图片跨集合。
- [x] 从 Manga109 已标注人脸框生成 `24×24` 正样本，并计算 11 通道。
- [x] 人工复核正样本裁剪，剔除不合格框；保留样本包含不同画风、脸型、发型、轻微旋转和遮挡。
- [x] 从真实漫画页面采集负样本，避开已标注人脸并过滤空白/纯黑区域，不使用随机噪声。
- [x] 人工复核负样本，剔除混入人脸及标签冲突的样本；保留真实漫画场景内容。
- [x] 只对训练集进行水平翻转增强，并使用固定随机种子。
- [x] 保存固定的数据划分文件，确认同一原图和作品的不同裁剪不跨集合。
- [x] 实现数据准备脚本，保存正负样本及其父图、原始坐标、来源和 split。
- [x] 生成所有正负样本的编号总览图，并完成人工逐张复核。

## 步骤四：训练 Depth-2 AdaBoost Cascade

- [x] 定义候选像素差特征：通道 `c` 与两个相对位置 `p1`、`p2`。
- [x] 实现 Depth-2 弱树，包括根节点、左右子节点和四个叶子分数。
- [x] 使用随机候选和贪心搜索训练弱树。
- [x] 使用 AdaBoost 更新样本权重并组合弱分类器。
- [x] 构建至少 3 个 Stage 的 Cascade。
- [x] 统计每个 Stage 的真实人脸保留情况和背景窗口拒绝情况。
- [x] 使用初始检测器扫描训练漫画页面，收集高分误检作为困难负样本并重新训练。
- [x] 展示至少 6 个困难负样本并解释误检原因。

## 步骤五：多尺度搜索与候选框合并

- [x] 构建图像金字塔，默认尺度增长约 1.2，并对比 1.1、1.2、1.3。
- [x] 记录每层缩放尺寸、滑窗数量、通过 Cascade 的候选数和处理时间。
- [x] 实现 `step=1` 滑动窗口，并与 `step=2` 比较结果和效率。
- [x] 将各尺度候选框还原到原图坐标。
- [x] 使用 NMS 合并相邻位置和尺度产生的重复候选框。

## 步骤六：28 点关键点回归

- [x] 已完成 anime256 固定 64 张的 CPU 预标注基准。
- [x] 固定 28 个关键点的编号和顺序，遮挡点使用 `visibility=0`；自动预标注在人工复核前仍为 `null`。
- [x] 已按固定 split 准备 320 张待复核自动预标注（train 240、validation 32、test 48），图片留在本地忽略目录。
- [x] 对 320 张完成 V/H/U 人工粗复核并接受现有预标注坐标；记录辅助复核来源与零坐标精调的限制。
- [x] 用户确认 320 张预标注已逐张核验且坐标无需修改；按此可用数据完成训练。六张全 U 自动排除训练目标和 NME，保留在固定 split 及逐图结果中。
- [x] 使用 anime-face-detector 预标注，并对 320 张进行 V/H/U 人工核验；坐标零改动，作为本实验可用的 28 点训练与评价参照。
- [x] 仅用 train 的粗复核可见点计算初始平均形状；坐标仍是未精调的 HRNetV2 预标注，详见 `results/landmark_mean_shape_coarse.json`。
- [x] 围绕关键点采样局部像素差特征。
- [x] 使用 Ridge Regression 预测 56 维形状残差并更新关键点。
- [x] 在同一固定 split、特征和随机种子下，对比遮挡点坐标全部屏蔽与纳入人工确认的可靠推定遮挡点；不可靠坐标始终屏蔽，并分别报告可见点 NME 与辅助的推定遮挡点误差。
- [x] 重复 4 轮完成多级形状回归。
- [x] 可选进阶：在同一固定 split 上尝试 Fern 风格局部二值特征；validation NME 未超过基础 Ridge，详见 `实验记录/2026-09-25_Fern进阶关键点对照.md`。

## 步骤七：评价指标

- [x] 检测使用 `IoU >= 0.5` 判定匹配，报告 Precision、Recall 和 F1；固定两张 validation 原生整页，见 `results/step7_detection_evaluation.json`。
- [x] 关键点报告 NME，使用双眼中心距离归一化，只统计 `visibility=1` 的点；双眼中心不可算时按规格说明回退人脸框对角线。
- [x] 记录平均每张图的检测时间和每个 Stage 的通过率；Stage 通过率基于步骤四固定裁剪测试集及步骤七固定抽样窗口，范围见阶段记录。
- [x] 记录不同滑窗步长的窗口数量和处理时间。
- [x] 比较整数版与浮点版输出是否一致；相同舍入规则下比较 100 个固定验证窗口。

## 步骤八：最终 Demo 与模型导出

- [x] 实现命令行 Demo：

```powershell
python demo.py --image test.jpg --model-dir models/ --output results/test_vis.jpg
```

- [x] 输出 Bounding Box、Detection Score、28 个关键点、可视化图片和结构化 JSON。
- [x] 实现统一接口 `AnimeFaceDetector.detect()`：输入 OpenCV BGR 图像，返回原图坐标结果并按分数降序排列，无检测时返回空列表。
- [x] 保存 11 通道定义、Depth-2 弱树参数、Cascade Stage 阈值、图像金字塔参数和候选框合并参数。
- [x] 保存关键点回归模型、关键点编号与归一化配置。
- [x] 保存数据划分和随机种子。

## 下一步：提升整页检测能力（用户明确要求）

当前 Demo 已可用，但八个固定 test 漫画作品上仍为 16 TP、149 FP、69 FN（Precision 0.097、Recall 0.188、F1 0.128）。误报包含非脸部漫画纹理，严格 HOG 筛选又造成明显漏检。诊断数值和限制见 `实验记录/2026-09-25_步骤八最终Demo与模型导出.md` 与 `results/step8_error_diagnosis.json`。以下是后续改进任务，不把它们误写为 PDF 步骤八的额外强制验收项。

- [ ] 固定当前模型、清单和 split；在 **train 页面**保存 NMS 前每个标注脸的最高候选 IoU、分数与尺度，再分别统计“未提出候选、定位不足、NMS 压掉、HOG/边长筛掉”。保留逐层窗口/候选计数和典型错误编号；用 validation 核对结论，不依据 test 调参。
- [ ] 从 train 页的非脸误报中分层抽取文字、头发、网点、图案和局部五官等难例；先排除可能未标注的人脸，制作小型联系图与复核 CSV，逐个确认后才加入负样本。保留被拒样本及理由、来源页、坐标、split 和种子。
- [ ] 用新增的已复核困难负样本重训或扩展 Cascade；同时检查每个 Stage 的人脸通过数、背景拒绝数，以及 validation 整页 IoU Precision/Recall/F1。不得仅靠提高 HOG 阈值换取低误报；比较召回与误报，并保留旧模型为历史对照。
- [ ] 若候选覆盖/定位仍是主要瓶颈，再研究窗口尺度、框形状或候选合并/定位修正；先在 validation 做消融并记录速度，最后用固定 test 作品评价锁定的配置。test 已被查看，结果不能称为完全盲测。
- [ ] 更新 `models/config.json`、Demo 效果图、相关统计和阶段记录；旧图表若不对应新配置，重跑或明确标为历史。交付前检查图片可打开、来源许可和 JSON/图注一致。

## 最终提交内容

- [ ] Python 源代码、`requirements.txt` 和 README 运行说明。
- [ ] 构建的人脸 28 点图片与标注。
- [ ] 模型文件与配置文件。
- [ ] 11 通道特征图和最终检测效果图。
- [ ] 项目报告。
