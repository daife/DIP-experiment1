# 实验一 TODO

本清单按实验说明的八个步骤排列，只保留明确要求及完成要求所需的最低验收项。

## 步骤一：环境搭建

- [x] 安装 `uv`。
- [x] 使用 CPython 3.12 创建项目虚拟环境 `.venv`。
- [x] 安装 `requirements.txt` 中的依赖。
- [x] 运行环境验证，确认 NumPy、OpenCV、scikit-learn、Matplotlib 和关键点依赖可用。

运行命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/setup_environment.ps1
```

## 步骤二：复现并观察 11 通道特征

- [x] 参考 `cpu-face-hpc` 实现 `compute_11_channels(gray)`。
- [x] 输入为 `uint8` 灰度图，输出 11 个同尺寸 `uint8` 通道：原图、2×2 局部均值、跨 2 像素平滑、4 个短距离方向梯度、4 个长距离方向梯度。
- [x] 中间计算使用 `int16` 或 `int32`，不使用现成卷积库直接生成全部结果。
- [x] 最低验收：检查通道数量、形状、类型，并用小矩阵抽查通道公式。
- [x] 选择至少 6 张图像：2 张真实正面人脸、2 张近正面动漫人脸、1 张头发遮挡、1 张漫画复杂背景。
- [x] 绘制每张图的“原图 + 11 个通道”。
- [x] 说明各通道突出哪些结构、容易受到哪些干扰，以及 Cascade 为什么能选出有效弱分类器。

## 步骤三：构建动漫人脸检测训练集

- [x] 已整理 Manga109、LAP 和 anime256，并生成统一图片清单与标准化人脸框。
- [x] 已按来源组固定 Train 75% / Validation 10% / Test 15%，避免同源图片跨集合。
- [x] 从 Manga109 已标注人脸框生成 `24×24` 正样本，并计算 11 通道。
- [x] 人工复核正样本裁剪，剔除不合格框；保留样本包含不同画风、脸型、发型、轻微旋转和遮挡。
- [x] 从真实漫画页面采集负样本，避开已标注人脸并过滤空白/纯黑区域，不使用随机噪声。
- [x] 人工复核负样本，剔除混入人脸及标签冲突的样本；保留真实漫画场景内容。
- [x] 只对训练集进行水平翻转增强，并使用固定随机种子。
- [x] 保存固定的数据划分文件，确认同一原图和作品的不同裁剪不跨集合。
- [x] 实现数据准备脚本，保存正负样本及其父图、原始坐标、来源和 split。
- [x] 生成所有正负样本的编号总览图，并抽查图像质量；人工逐张复核仍待完成。

## 步骤四：训练 Depth-2 AdaBoost Cascade

- [x] 定义候选像素差特征：通道 `c` 与两个相对位置 `p1`、`p2`。
- [x] 实现 Depth-2 弱树，包括根节点、左右子节点和四个叶子分数。
- [x] 使用随机候选和贪心搜索训练弱树。
- [ ] 使用 AdaBoost 更新样本权重并组合弱分类器。
- [ ] 构建至少 3 个 Stage 的 Cascade。
- [ ] 统计每个 Stage 的真实人脸保留情况和背景窗口拒绝情况。
- [ ] 使用初始检测器扫描训练漫画页面，收集高置信度误检作为困难负样本并重新训练。
- [ ] 展示至少 6 个困难负样本并解释误检原因。

## 步骤五：多尺度搜索与候选框合并

- [ ] 构建图像金字塔，默认尺度增长约 1.2，并对比 1.1、1.2、1.3。
- [ ] 记录每层缩放尺寸、滑窗数量、通过 Cascade 的候选数和处理时间。
- [ ] 实现 `step=1` 滑动窗口，并与 `step=2` 比较结果和效率。
- [ ] 将各尺度候选框还原到原图坐标。
- [ ] 使用 NMS 或 Similar Rectangles 合并相邻位置和尺度产生的重复候选框。

## 步骤六：28 点关键点回归

- [x] 已完成 anime256 固定 64 张的 CPU 预标注基准。
- [ ] 固定 28 个关键点的编号和顺序，遮挡点使用 `visibility=0`。
- [ ] 使用 anime-face-detector 预标注并进行人工修正，形成可用的 28 点数据。
- [ ] 计算初始平均形状。
- [ ] 围绕关键点采样局部像素差特征。
- [ ] 使用 Ridge Regression 预测 56 维形状残差并更新关键点。
- [ ] 重复 3～5 轮完成多级形状回归。
- [ ] 可选：根据基础方案效果尝试 Fern/LBF 风格特征或可见点加权损失。

## 步骤七：评价指标

- [ ] 检测使用 `IoU >= 0.5` 判定匹配，报告 Precision、Recall 和 F1。
- [ ] 关键点报告 NME，使用双眼中心距离归一化，只统计 `visibility=1` 的点。
- [ ] 记录平均每张图的检测时间和每个 Stage 的通过率。
- [ ] 记录不同滑窗步长的窗口数量和处理时间。
- [ ] 比较整数版与浮点版输出是否一致。

## 步骤八：最终 Demo 与模型导出

- [ ] 实现命令行 Demo：

```powershell
python demo.py --image test.jpg --model-dir models/ --output results/test_vis.jpg
```

- [ ] 输出 Bounding Box、Detection Score、28 个关键点、可视化图片和结构化 JSON。
- [ ] 实现统一接口 `AnimeFaceDetector.detect()`：输入 OpenCV BGR 图像，返回原图坐标结果并按分数降序排列，无检测时返回空列表。
- [ ] 保存 11 通道定义、Depth-2 弱树参数、Cascade Stage 阈值、图像金字塔参数和候选框合并参数。
- [ ] 保存关键点回归模型、关键点编号与归一化配置。
- [ ] 保存数据划分和随机种子。

## 最终提交内容

- [ ] Python 源代码、`requirements.txt` 和 README 运行说明。
- [ ] 构建的人脸 28 点图片与标注。
- [ ] 模型文件与配置文件。
- [ ] 11 通道特征图和最终检测效果图。
- [ ] 项目报告。
