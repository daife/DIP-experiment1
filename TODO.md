# 实验一 TODO（按实验步骤）

本文档严格按照实验说明中的八个步骤安排工作。所有派生样本必须继承 `datasets/manifests/images.csv` 中的 `source_group` 与 `split`，禁止对裁剪结果重新随机划分；原始图片与源标签保持只读。

## 步骤一：环境搭建

- [x] 使用 `uv` 安装并固定 CPython 3.12。
- [x] 使用 `uv` 创建项目虚拟环境 `.venv`。
- [x] 在 `requirements.txt` 中声明 NumPy、OpenCV、scikit-learn、Matplotlib、Pillow、tqdm 和 anime-face-detector 等依赖。
- [x] 实现 `scripts/setup_environment.ps1`，支持环境创建、依赖安装和重复执行。
- [x] 实现 `scripts/verify_environment.py`，验证灰度转换、整数像素运算、Depth-2 决策树、无界面绘图及关键点依赖。
- [x] 在 Windows 11、CPython 3.12.13 环境完成验收，所有检查通过。

验收命令：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/setup_environment.ps1
```

## 步骤二：复现并观察 11 通道特征

### 2.1 特征实现

- [ ] 新建特征模块，实现 `compute_11_channels(gray)`。
- [ ] 输入只接受二维 `uint8` 灰度图，输出固定为 11 个同尺寸 `uint8` 通道。
- [ ] 按参考工程复现：原始灰度、2×2 局部均值、跨 2 像素平滑、4 个短距离方向梯度、4 个长距离方向梯度。
- [ ] 中间计算使用 `int16` 或 `int32`，禁止依赖 `uint8` 溢出行为。
- [ ] 明确并固定边界填充、取整、截断和梯度映射规则。
- [ ] 不使用现成卷积函数一次性生成全部通道。

### 2.2 正确性测试

- [ ] 为常量图、水平/垂直渐变图、棋盘图和随机图编写单元测试。
- [ ] 验证所有通道的形状、类型和 `[0, 255]` 数值范围。
- [ ] 用手工计算的小矩阵核对每个通道的像素值。
- [ ] 对照 `cpu-face-hpc/face_frontal.py` 检查公式和边界行为。
- [ ] 记录整数实现与等价浮点参考实现的差异。

### 2.3 可视化与观察

- [ ] 选择至少 6 张图：2 张真实正面人脸、2 张近正面动漫脸、1 张头发遮挡脸、1 张复杂漫画背景。
- [ ] 为每张图输出“原图 + 11 通道”并统一色阶和通道编号。
- [ ] 记录哪些通道突出眼睛、鼻口、脸部轮廓，哪些通道易响应头发、对白框和网点。
- [ ] 写实验记录，解释局部平滑、像素差的亮度鲁棒性以及 Cascade 可选出有效特征的原因。

## 步骤三：构建动漫人脸检测训练集

### 3.1 固定配置与可追溯性

- [ ] 新建 `configs/data.yaml`，记录随机种子、数据版本、裁剪扩展比例、插值方式、正负样本数、IoU 阈值及 LAP 自动框阈值。
- [ ] 首版数据命名为 `detection_v1`；改变阈值或采样方法时新建版本，禁止覆盖旧版本。
- [ ] 所有生成脚本支持 `--seed`、`--split`、`--output-dir`，并输出完整运行元数据。
- [x] 已按 `source_group` 固定 Train 75% / Validation 10% / Test 15% 划分。
- [x] 已生成统一图片清单、来源组清单及标准化 LAP/Manga109 人脸框。
- [x] 已确认同一 `source_group` 不跨 split。

建议目录：

```text
datasets/derived/detection_crops/detection_v1/
├── positives/
├── negatives/
├── manifests/
└── previews/
```

### 3.2 正样本

- [ ] Manga109 使用 `manga109_faces.jsonl` 中的人工框。
- [ ] LAP 前 5,000 张优先使用 `lap_faces_manual.jsonl`，不与同图自动框重复叠加。
- [ ] 对 LAP 自动框的 `0.80～0.90`、`0.90～0.95`、`>=0.95` 三档各抽查至少 100 个，再锁定首版阈值。
- [ ] anime256 作为整图单脸补充池，并与框裁剪样本分开统计。
- [ ] 人脸框以中心扩展约 10%，补为正方形，裁到合法边界后缩放为 `24×24`。
- [ ] 保存原始框、扩展框、有效裁剪框、父图 ID、数据源、来源组、split 和标注来源。
- [ ] 过滤空框、负宽高框、读取失败图片和无法辨认的严重放大小脸，异常必须写日志。
- [ ] 只对 train 做水平翻转、`±10°` 小旋转、轻微平移缩放、亮度/对比度和模糊增强。
- [ ] 使用像素哈希或感知哈希检查重复裁剪。

### 3.3 普通负样本

- [ ] 从 Manga109 页面和 LAP 海报采样真实背景，不使用随机噪声代替。
- [ ] 内容覆盖头发、衣服、手部、文字框、拟声词、建筑、物体和局部类脸结构。
- [ ] 按正样本人脸尺寸分布采样多尺度正方形窗口并缩放为 `24×24`。
- [ ] 与所有已知或疑似人脸框的 IoU 保持 `<0.10`，并排除中心落入人脸安全扩展区的窗口。
- [ ] LAP 使用低至 `0.50` 的自动框构造负样本禁采区，降低漏标脸混入负类的风险。
- [ ] 每张原图限制 20～50 个负样本，首版比较负:正为 `1:1`、`3:1`、`5:1`。
- [ ] validation/test 生成固定且不再变化的负样本集合。

### 3.4 数据脚本与验收

- [ ] 实现 `scripts/prepare_detection_data.py`，一次生成正样本、普通负样本和清单。
- [ ] 清单至少包含 `sample_id`、`label`、`sample_path`、`parent_image_id`、`source_dataset`、`source_group`、`split`、源框、裁剪框、标注来源、置信度、生成方法和数据版本。
- [ ] 每个数据源、每个 split 至少生成一张固定网格预览图。
- [ ] 人工抽查至少 500 个普通负样本，并单独检查靠近人脸的边界样本。
- [ ] 输出样本数、框尺寸分布、过滤原因、每图采样数、重复率和来源分布。
- [ ] 自动检查图片可读、尺寸为 `24×24`、标签合法、sample ID 唯一且无 split 泄漏。
- [ ] 为配置和清单生成 SHA-256，并保存 `dataset_summary.json`。

## 步骤四：训练 Depth-2 AdaBoost Cascade

### 4.1 弱分类器与 AdaBoost

- [ ] 定义像素差候选特征：通道 `c`、相对位置 `p1` 与 `p2`。
- [ ] 实现 Depth-2 弱树：根节点、左右子节点及四个叶子分数。
- [ ] 用随机候选加贪心搜索选择节点特征和阈值。
- [ ] 实现 AdaBoost 样本权重更新、弱树权重和强分类器分数。
- [ ] 为树路由、阈值边界、权重归一化和模型序列化编写测试。

### 4.2 多阶段 Cascade

- [ ] 先训练可快速运行的 Stage 0/1 原型，验证数据与训练流程。
- [ ] 扩展到至少 3 个 Stage，并为每个 Stage 设置通过阈值。
- [ ] 记录每个 Stage 的正样本保留率、负样本拒绝率和剩余窗口数。
- [ ] 若 Stage 0 大量漏脸，优先修正阈值、特征或正样本；若末级背景过多，优先补充负样本。
- [ ] 保存每轮模型、训练配置、数据版本、随机种子和训练指标。

### 4.3 困难负样本挖掘

- [ ] 实现 `scripts/mine_hard_negatives.py`，只扫描 split=`train` 的 Manga109/LAP 场景。
- [ ] 使用与最终推理一致的图像金字塔、滑窗步长、11 通道和 Cascade 阈值。
- [ ] `max IoU <0.10` 才可直接作为负候选；`0.10～0.30` 进入人工复核；`>=0.30` 排除。
- [ ] 检测中心落在人脸安全区时排除，避免把真脸局部错误标成负类。
- [ ] 同图候选先以 IoU 0.50 做 NMS，每图最多保留分数最高的 20 个。
- [ ] 每轮困难负样本数量控制在正样本量的 0.5～1.0 倍，并用感知哈希去重。
- [ ] 最多执行 2～3 轮；若 validation 每图误报下降不足 5% 或 Recall 明显下降则停止。
- [ ] 每轮保存至少 6 个代表性困难负样本并解释误检原因。

## 步骤五：多尺度搜索与候选框合并

- [ ] 实现约 1.2 倍尺度增长的图像金字塔，并对比 1.1、1.2、1.3。
- [ ] 每层记录缩放尺寸、滑窗数量、各 Stage 通过数及耗时。
- [ ] 从 `step=1` 逐像素扫描开始，再与 `step=2` 比较速度和召回率。
- [ ] 正确还原不同金字塔层的候选框到原图坐标。
- [ ] 实现 NMS 或 Similar Rectangles 聚类，合并相邻位置和尺度的重复检测。
- [ ] 为尺度映射、边界窗口、NMS/聚类和空结果编写测试。

## 步骤六：28 点关键点回归

### 6.1 标注数据

- [x] 已完成 anime256 固定 64 张的 uv/CPU 预标注吞吐量基准。
- [ ] 在标注规范确定后运行 anime256 train 全量自动预标注。
- [ ] 制作固定的 28 点编号示意图和人工修正规范。
- [ ] 被遮挡或不可判断点使用 `visibility=0`；模型 `confidence` 与人工可见性分开保存。
- [ ] 按低响应、点越界、极端姿态、遮挡和画风分层安排人工复核。
- [ ] 安排一小批双人重复标注，检查关键点编号理解是否一致。
- [ ] 只有人工确认的 `corrected` 标注可作为回归真值。

### 6.2 多级形状回归

- [ ] 计算训练集平均初始形状并归一化到人脸框。
- [ ] 围绕当前关键点采样局部像素差特征。
- [ ] 使用 Ridge Regression 预测 56 维形状残差。
- [ ] 重复 3～5 轮形状更新，并保存每一级模型。
- [ ] 仅对 `visibility=1` 的点计算训练损失和评估误差。
- [ ] 视基础方案结果决定是否实现 Fern/LBF 风格特征或可见点加权损失。

## 步骤七：评价指标

- [ ] 检测匹配固定使用 `IoU >=0.5`，报告 Precision、Recall、F1 和每图误报数。
- [ ] 关键点报告 NME，只统计 `visibility=1` 的点，默认用双眼中心距离归一化。
- [ ] 报告平均每图检测时间、各 Stage 通过率、各尺度窗口数和不同步长耗时。
- [ ] 验证整数版与浮点参考版输出是否一致，并记录允许误差。
- [ ] 在固定 validation/test 上比较普通负样本与加入困难负样本后的结果。
- [ ] 保存结构化指标 JSON、汇总表和可直接用于报告的图表。

## 步骤八：最终 Demo 与模型导出

- [ ] 实现统一接口 `AnimeFaceDetector.detect()`，输入 OpenCV BGR 图像，输出原图坐标结果并按分数降序排列；无检测时返回空列表。
- [ ] 实现命令行 Demo：

```powershell
python demo.py --image test.jpg --model-dir models/ --output results/test_vis.jpg
```

- [ ] 输出 Bounding Box、Detection Score、28 个关键点、可视化图片和结构化 JSON。
- [ ] 导出 11 通道定义、Depth-2 弱树参数、Stage 阈值、金字塔参数及候选框合并参数。
- [ ] 导出关键点回归模型、关键点编号、可见性及归一化配置。
- [ ] 保存数据划分、数据版本、随机种子和清单校验值。
- [ ] 用全新环境按 README 从零运行一次最终 Demo。

## 最终提交检查

- [ ] 完整 Python 源代码、`requirements.txt` 和 README 运行说明。
- [ ] 人脸 28 点图片与人工确认标注。
- [ ] 最终模型文件和完整配置。
- [ ] 11 通道图、困难负样本图和最终检测效果图。
- [ ] 检测、关键点和效率指标。
- [ ] 项目报告，包含特征解释、失败案例、消融实验和结论。
